# ------------------- 用户配置 -------------------

# 1. 根目录：存放所有原始实验数据（视频和CSV）的顶层文件夹路径
# 请使用 r"..." 格式，避免路径中的斜杠问题
ROOT_DIR = r"D:\new_data"

# 2. 图片尺寸：所有从视频中提取的帧都会被统一调整到这个尺寸
IMAGE_SIZE = (640, 480)

# 3. 视频文件扩展名 (注意大小写，统一使用小写)
VIDEO_EXTENSION = ".avi"

# 4. CSV 文件名前缀 (用于查找匹配的CSV，统一使用小写)
CSV_PREFIX = "data_"

# 5. 视频文件名前缀 (用于从视频名中提取实验ID，统一使用小写)
# 例如，如果视频名为 "video_01.avi"，这里就填 "video_"
VIDEO_PREFIX_TO_REPLACE = "video_"
